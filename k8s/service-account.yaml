apiVersion: v1
kind: ServiceAccount
metadata:
  name: litellm-wif-sa
  namespace: agentic
  annotations:
    # Explicit Workload Identity binding back to your GCP project
    iam.gke.io/gcp-service-account: litellm-gateway-sa@alert-hall-466720-c0.iam.gserviceaccount.com
