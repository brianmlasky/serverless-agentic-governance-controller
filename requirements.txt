# Use a modern, compatible base for FastAPI/Starlette
fastapi==0.111.0
uvicorn==0.30.1
starlette==0.37.2

# Networking & Telemetry
httpx==0.27.0
prometheus-client==0.20.0
pydantic==2.7.4

# Critical Security Patches (Explicitly overriding vulnerable sub-deps)
# These versions are known safe and compatible with the above
wheel==0.46.2
jaraco.context==6.1.0