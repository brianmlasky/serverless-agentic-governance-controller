# Use a specific FastAPI version; let pip resolve the internal starlette dependency
fastapi==0.111.0
uvicorn==0.30.1

# Networking & Telemetry (Unpinned or wide-range for resolver)
httpx>=0.27.0
prometheus-client>=0.20.0
pydantic>=2.7.0

# Security Overrides: These are the specific packages Trivy flagged.
# Forcing these will force the resolver to find a compatible graph.
wheel>=0.46.2
jaraco.context>=6.1.0