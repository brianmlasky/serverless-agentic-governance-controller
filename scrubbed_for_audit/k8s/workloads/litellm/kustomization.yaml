apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: agentic
resources:
  - configmap.yaml
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
spec:
  template:
    spec:
      containers: []
  jobTemplate:
    spec:
      template:
        spec:
          containers: []
  containers: []
