apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sagc-controller-monitor
  namespace: agentic
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: sagc-controller
  endpoints:
  - port: metrics
    interval: 15s
    path: /metrics
